/**
 * BlueprintPanelManager — legacy floating panel (superseded by BlueprintViewProvider sidebar).
 * Kept as an empty stub so the file doesn't cause build errors.
 * The active implementation is in src/panels/BlueprintViewProvider.ts
 */
export { };
